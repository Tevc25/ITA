name: Gateway Mobile CI

on:
  push:
    paths:
      - 'gateway-mobile/**'
      - '.github/workflows/gateway-mobile-ci.yml'
  pull_request:
    paths:
      - 'gateway-mobile/**'
      - '.github/workflows/gateway-mobile-ci.yml'

jobs:
  unit-test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: gateway-mobile

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25.x'

      - name: Download dependencies
        run: go mod download

      - name: Run tests
        run: go test ./... -v
